• Wind forecasts for your sites load noticeably faster - requests now run in
  parallel and caches are warmed as the app starts, so wind is ready when you
  open a site instead of arriving after it
• Fixed the splash screen overflowing on the very first frame on desktop
